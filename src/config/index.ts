// Public configuration (safe for client)
export { publicEnv } from "./env";
export { siteConfig } from "./site";

// Server configuration is NOT exported here
// Import directly: import { serverEnv } from "@/config/env.server"
